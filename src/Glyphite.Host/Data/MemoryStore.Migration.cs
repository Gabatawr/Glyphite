namespace Glyphite.Host.Data;

public partial class MemoryStore
{
    // ── Database schema is defined in Initialize() via CREATE TABLE IF NOT EXISTS ──
    // If you need to add columns or tables in the future, add migration methods here
    // and call them from Initialize() after the CREATE TABLE statements.
}
